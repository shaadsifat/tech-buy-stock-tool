# WAL sidecar files change constantly while the app runs — not worth tracking,
# and can conflict across machines. The main app.db file IS tracked (by choice).
data/*.db-wal
data/*.db-shm

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS junk
Thumbs.db
.DS_Store

# Claude Code project-local settings (if any get created here later)
.claude/

# Shopify Storefront API credentials — never committed
.env
get_product_api_call.py
